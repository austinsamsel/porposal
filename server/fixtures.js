if (Posts.find().count() === 0) {
  Posts.insert({
    title: 'Klast'
  });

  Posts.insert({
    title: 'Davler'
  });

  Posts.insert({
    title: 'BKFP'
  });

  Posts.insert({
    title: 'Creators'
  });

}